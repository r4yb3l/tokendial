# Tokendial

**Read `AGENTS.md` before changing anything.** It carries the layout, the build and test commands,
the house rules and the things that are true and non-obvious about this repo.

Two more, when they apply:

- `tasks/lessons.md` — every entry is a mistake that reached a user or burned an afternoon. Read it
  before a second attempt at anything.
- `tasks/todo.md` — what is done, what is open, and pointers to the parked research.
